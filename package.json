{
  "name": "swords_and_sandcastles",
  "version": "0.0.1",
  "dependencies": {
    "electron": "^1.6.6",
    "phaser": "^2.6.2",
    "rollup": "^0.41.6",
    "rollup-plugin-node-resolve": "^3.0.0"
  },
  "scripts": {
    "dev": "npm run tsWatch & npm run rollupWatch & npm run serve",
    "devElectron": "npm run tsWatch & npm run rollupWatch & npm run electronServe",
    "rollup": "rollup -c \"rollup.config.js\"",
    "build": "tsc && npm run rollup",
    "tsWatch": "tsc -w",
    "rollupWatch": "rollup -c \"rollup.config.js\" -w",
    "serve": "./node_modules/http-server/bin/http-server ./ -p 8888",
    "electronServe": "electron ."
  },
  "main": "main.js",
  "devDependencies": {
    "concurrently": "^3.5.0",
    "http-server": "^0.10.0",
    "rollup-watch": "^4.3.1"
  }
}
