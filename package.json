{
  "name": "ts-electron-phaser-boilerplate",
  "version": "0.0.1",
  "dependencies": {
    "electron": "^3.0.2",
    "parcel": "^1.10.1",
    "phaser": "^3.14.0",
    "typescript": "^3.1.1"
  },
  "scripts": {
    "dev": "parcel index.html",
    "devElectron": "npm run tsWatch & npm run dev & npm run electronServe",
    "build": "parcel build ./index.html -d build/",
    "tsWatch": "tsc -w",
    "electronServe": "electron ."
  },
  "main": "main.js",
  "devDependencies": {
    "concurrently": "^4.0.1",
    "http-server": "^0.11.1",
    "parcel-bundler": "^1.10.1",
    "prettier": "^1.14.3",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0"
  }
}
